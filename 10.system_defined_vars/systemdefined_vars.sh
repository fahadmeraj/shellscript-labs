#System defined variables

echo ${SHELL}
echo ${HOME}
echo ${OSTYPE}
echo ${$} # process ID
echo ${PPID} # parent process ID

echo ${PWD}
echo ${UID}
echo ${HOSTNAME}

UID="5000"
echo ${UID}
# System defined variable value can be changed if it is not readonly var such as PATH variable

# sleep 5

echo ${SECONDS}

